<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>To-Do Dashboard</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 20px;
    }
    .todo-container {
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      max-width: 100%;
      margin: auto;
    }
    .todo-container h1 {
      color: #0098CA;
    }
    .todo-list {
      margin-top: 20px;
    }
    .todo-list table {
      width: 100%;
      border-collapse: collapse;
    }
    .todo-list th, .todo-list td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    .todo-list th {
      background-color: #f2f2f2;
    }
    .footer {
      text-align: center;
      margin-top: 20px;
      font-size: 12px;
      color: #333;
    }
    .footer a {
      color: #0098CA;
      text-decoration: none;
    }
    .footer a:hover {
      text-decoration: underline;
    }

    .task-input-container {
    display: flex;
    flex-direction: column;
    gap: 10px;
    max-width: 400px;
    margin: auto;
    padding: 15px;
    background: #f9f9f9;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }

  .task-label {
    font-size: 14px;
    font-weight: bold;
    color: #333;
  }

  textarea {
    width: 100%;
    padding: 10px;
    border: 2px solid #ddd;
    border-radius: 5px;
    font-size: 14px;
    resize: none;
    transition: border-color 0.3s ease;
  }

  textarea:focus {
    border-color: #0098CA;
    outline: none;
  }

  button {
    padding: 10px;
    background-color: #0098CA;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 14px;
    transition: background 0.3s ease;
  }

  button:hover {
    background-color: #0077A3;
  }
  </style>
</head>
<body>

  <div class="todo-container">
    <h1>Welcome to Your To-Do Dashboard</h1>
    <p>Manage your tasks efficiently. Add, update, and complete your to-dos.</p>

    <div class="todo-list">
      <h2>Your To-Do List</h2>

      <div class="task-input-container">
        <label for="taskadd" class="task-label">New Task:</label>
        <textarea id="taskadd" placeholder="Write your task here..." rows="2"></textarea>
        <button id="addTaskButton">➕ Add Task</button>
      </div>

      <div>

      </div>

    </div>

    <div class="footer">
      <p><a href="/logout">Logout</a></p>
    </div>
  </div>

</body>
</html>
